rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{folder}/{allPaths=**} {
      allow read: if folder in ['posts', 'cases', 'research'];
      allow write: if request.auth != null
        && folder in ['posts', 'cases', 'research'];
    }
  }
}
